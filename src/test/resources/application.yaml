environment: unittest
yatt:
  datasource.schema: yatt

spring:
  jpa.database-platform: org.hibernate.dialect.PostgreSQLDialect
  main:
    allow-bean-definition-overriding: true
  mvc:
    converters:
      preferred-json-mapper: jackson

